#ifndef SEPARATOR
#define SEPARATOR

#include <Arduino.h>
#include <iostream> 

using namespace std;
class Separator
{
    public:
        explicit Separator();
        String* SeparatorIndex(String _array,String Element);

    protected:

    private: 
};
#endif

